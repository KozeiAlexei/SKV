@using SKV.PL.ClientSide.Concrete
@using SKV.PL.ClientSide.Components
@using SKV.PL.ClientSide.Components.Tabs
@using SKV.PL.ClientSide.Components.Modal

@(ComponentFactory.Create<ModalMvc>()
        .Id(ClientSideConfiguration.ConfirmationModalName)
        .Title(Resources.Resource.ActionConfirmation)
        .Body(body => body.Create<ContentMvc>().FromPartitalView(Html.Partial("ConfirmationModalBody")))
        .ManualClosing(true)
        .Render()
)
