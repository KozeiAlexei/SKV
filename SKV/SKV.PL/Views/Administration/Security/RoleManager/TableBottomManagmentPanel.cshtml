@using Config = SKV.Configuration

@{ 
    var ctrl = Config.AngularControllerAs.RoleManagerControllerAs;
}

<div>
    <button class="btn btn-success btn-flat" ng-click="@(ctrl).ShowUserCreationModal()">
        <span class="glyphicon glyphicon-plus">
    </button>
</div>